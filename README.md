ALGORTIHM:
STEP 1-> initlaize int freq that tells characters of strings and char str[];
step 2->  use first loop -> freq[str[i++]] to store frequency of characters and increment also.
STEP 3-> use second loop ->  str[i]!=0 then print characters.
step 4=> freq[str[i]]=0 reset frequency and to avoid duplicates. 
